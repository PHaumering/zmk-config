## German Umlauts

Following [this discussion](https://github.com/nickcoutsos/keymap-editor/discussions/203), one just has to add `keys_de.h` from the [zmk-locale-generator releases](https://github.com/joelspadin/zmk-locale-generator/release) to the `\config` directory and include it in the `.keymap` file.
